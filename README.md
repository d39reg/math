# math

Commands:
1) `+` this symbol allows sum numbers
2) `-` this symbol allows subtract numbers
3) `*` this symbol allows multiply numbers
4) `:` this symbol allows divide numbers
5) `(` and `)` this symbols increase the priority of the operation

Numbers look like:
`123` for integers
and `1,23` for float
